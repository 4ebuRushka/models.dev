# Sources:
# https://openrouter.ai/meta/muse-spark-1.2
# https://openrouter.ai/api/v1/models/meta/muse-spark-1.2/endpoints
# Pricing: $1.25 / $4.25 per 1M; cache read $0.15 (Meta endpoint via OpenRouter)
# Effort: reasoning_effort supported (minimal|low|medium|high|xhigh; matches Meta first-party)
base_model = "meta/muse-spark-1.2"

reasoning_options = [{ type = "effort", values = ["minimal", "low", "medium", "high", "xhigh"] }]

[cost]
input = 1.25
output = 4.25
cache_read = 0.15
