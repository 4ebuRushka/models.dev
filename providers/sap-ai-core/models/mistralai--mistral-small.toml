name = "mistralai--mistral-small"
base_model = "mistral/mistral-small-2603"
last_updated = "2026-07-14"
# SAP orchestration natively maps the listed reasoning effort values, even if the model does not natively support them.
reasoning_options = [{ type = "effort", values = ["none", "minimal", "low", "medium", "high"] }]
attachment = true
open_weights = true

[cost]
input = 0.07
output = 0.28

[limit]
context = 128_000

[modalities]
input = ["text", "image"]
output = ["text"]
